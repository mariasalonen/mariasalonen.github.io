import React from "react";
import { Container } from "react-bootstrap";

const Cv = () => {
  return (
    <>
      <Container>This is CV component inside a container</Container>
    </>
  );
};

export default Cv;
